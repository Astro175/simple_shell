#include "main.h"
/**
 * main - main
 * @argc: arg count
 * @argv: arg vector
 * Return: exit success
 */
int main(int argc, char **argv)
{
	(void)(argc);
	(void)(argv);

	shl_loop();

	return (EXIT_SUCCESS);
}
